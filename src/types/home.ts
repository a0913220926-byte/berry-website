export type Service = {
  title: string;
  description: string;
  capabilities: string[];
};

export type Work = {
  title: string;
  category: string;
  description: string;
  image: string;
};

export type ProcessStep = {
  phase: string;
  title: string;
  description: string;
};
